import * as types from './mutations'

export const doSomething = ({ commit }, character) => {
  /*
  if (character.inventory > 0) {
    commit(types.DO_SOMETHING, {
      id: character.id
    })
  }
  */
}